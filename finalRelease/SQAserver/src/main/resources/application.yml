spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SQAserver?createDatabaseIfNotExist=true
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
server:
  port: 8085
security:
  oauth2:
    resource:
      token-info-uri: http://localhost:8080/authenticate
    client:
      client-id: i1
      client-secret: ishan
